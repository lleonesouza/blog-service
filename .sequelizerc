const path = require('path')

module.exports = {
    // yarn sequelize db:create
    config: path.resolve(__dirname, 'src', 'config.js' ),
    // yarn sequelize migration:create --name=my_migrations
    "migrations-path": path.resolve(__dirname, 'src', 'db', 'sequelize', 'migrations')
    // yarn sequelize db:migrate:undo 
    // yarn sequelize migration:create --name=add-cellphone-to-user_db
    }